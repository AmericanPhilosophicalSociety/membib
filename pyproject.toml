[project]
name = "membib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7",
    "django-edit-away>=0.1.2",
    "dotenv>=0.9.9",
    "ruff>=0.16.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.ruff.format]
quote-style = "single"
